export interface IBook {
  _id: number;
  title: string;
  image: string;
  publication_date: string;
  genre: string;
  author: string;
  // features: string[];
  status: boolean;
  // rating: number;
  // quantity?: number;
}
